<form method="post" action="{{ path('news_delete', {'id': news.id}) }}"
      onsubmit="return confirm('Вы уверены, что хотите удалить элемент?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news.id) }}">
    <button type="submit" class="btn btn-danger btn_more">Удалить</button></p>
</form>
